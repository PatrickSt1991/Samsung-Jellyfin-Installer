name: Beta Pre-Release

on:
  push:
    branches: [beta]

permissions:
  contents: write

env:
  PRODUCT_NAME: Jellyfin2Samsung
  PROJECT_DIR: Jellyfin2Samsung-CrossOS
  CONFIGURATION: Release

jobs:
  beta-release:
    runs-on: ubuntu-latest

    steps:
      # --------------------------------------------------
      # Checkout (full history needed for changelog & tags)
      # --------------------------------------------------
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # --------------------------------------------------
      # Extract version from AppSettings.cs
      # --------------------------------------------------
      - name: Extract version
        shell: bash
        run: |
          VERSION=$(curl -fsSL \
            https://raw.githubusercontent.com/Jellyfin2Samsung/Samsung-Jellyfin-Installer/refs/heads/beta/Jellyfin2Samsung-CrossOS/Helpers/AppSettings.cs \
            | grep -oE 'v[0-9]+(\.[0-9]+){1,3}')

          if [ -z "$VERSION" ]; then
            echo "❌ Failed to extract version"
            exit 1
          fi

          echo "VERSION=${VERSION#v}" >> $GITHUB_ENV
          echo "VERSION_TAG=${VERSION}-beta" >> $GITHUB_ENV

      # --------------------------------------------------
      # Decide whether to skip beta release (ONLY if stable exists)
      # --------------------------------------------------
      - name: Decide whether to skip beta release
        shell: bash
        run: |
          SKIP=false

          STABLE_TAG=$(git tag \
            | grep -E '^v[0-9]+(\.[0-9]+){1,3}$' \
            | sort -V \
            | tail -n 1)

          if [ -n "$STABLE_TAG" ] && [ "${STABLE_TAG#v}" = "$VERSION" ]; then
            echo "ℹ️ Stable version $STABLE_TAG already exists — beta disabled"
            SKIP=true
          fi

          echo "SKIP_RELEASE=$SKIP" >> "$GITHUB_ENV"

      # --------------------------------------------------
      # Generate changelog
      # --------------------------------------------------
      - name: Generate changelog
        if: env.SKIP_RELEASE != 'true'
        shell: bash
        run: |
          LAST_TAG=$(git tag --sort=-creatordate | grep beta | head -n 1)

          if [ -z "$LAST_TAG" ]; then
            RANGE="HEAD"
          else
            RANGE="$LAST_TAG..HEAD"
          fi

          {
            echo "## ✅ What's New & Improved"
            git log $RANGE --pretty=format:"- %s"
          } > CHANGELOG.md

      # --------------------------------------------------
      # Prepare release notes
      # --------------------------------------------------
      - name: Prepare release notes
        if: env.SKIP_RELEASE != 'true'
        shell: bash
        run: |
          DATE=$(date +'%Y-%m-%d')

          {
            echo "## 📦 [${VERSION_TAG}] – ${DATE}"
            echo ""
            cat CHANGELOG.md
            echo ""
            echo "---"
            echo ""
            echo "⚠️ **Disclaimer — Platform Support Status**"
            echo ""
            echo "| Platform | Status | Notes |"
            echo "|----------|--------|-------|"
            echo "| 🍎 macOS | ⚠️ Beta | CI-built |"
            echo "| 🐧 Linux | ⚠️ Beta | CI-built |"
            echo "| 🪟 Windows | ⚠️ Beta | CI-built |"
            echo ""
            echo "---"
            echo ""
            echo "### 🛡️ Security Notice"
            echo "Antivirus warnings may occur and are likely **false positives**."
            echo ""
            echo "---"
            echo ""
            echo "### 🙌 Thanks for testing **Samsung Jellyfin Installer (Beta)**"
            echo "💬 Feedback welcome via GitHub issues & discussions."
          } > RELEASE_NOTES.md

      # --------------------------------------------------
      # Setup .NET SDK
      # --------------------------------------------------
      - uses: actions/setup-dotnet@v4
        if: env.SKIP_RELEASE != 'true'
        with:
          dotnet-version: 8.0.x

      # --------------------------------------------------
      # Publish (Linux runner, cross-compile)
      # --------------------------------------------------
      - name: Publish Windows
        if: env.SKIP_RELEASE != 'true'
        run: |
          dotnet publish \
            -c $CONFIGURATION \
            -r win-x64 \
            -p:SelfContained=true \
            -p:UseAppHost=true \
            -o publish/win-x64

      - name: Publish macOS
        if: env.SKIP_RELEASE != 'true'
        run: |
          dotnet publish \
            -c $CONFIGURATION \
            -r osx-x64 \
            -p:SelfContained=true \
            -p:UseAppHost=true \
            -o publish/osx-x64

      - name: Publish Linux
        if: env.SKIP_RELEASE != 'true'
        run: |
          dotnet publish \
            -c $CONFIGURATION \
            -r linux-x64 \
            -p:SelfContained=true \
            -p:UseAppHost=true \
            -o publish/linux-x64

      # --------------------------------------------------
      # Package artifacts (clean layout)
      # --------------------------------------------------
      - name: Package artifacts
        if: env.SKIP_RELEASE != 'true'
        shell: bash
        run: |
          mkdir -p dist

          (
            cd publish/win-x64
            zip -r "../../dist/${PRODUCT_NAME}-${VERSION_TAG}-win-x64.zip" .
          )

          tar -czf "dist/${PRODUCT_NAME}-${VERSION_TAG}-osx-x64.tar.gz"   -C publish/osx-x64 .
          tar -czf "dist/${PRODUCT_NAME}-${VERSION_TAG}-linux-x64.tar.gz" -C publish/linux-x64 .

      # --------------------------------------------------
      # Create or Update GitHub Pre-Release (mutable beta)
      # --------------------------------------------------
      - name: Create or Update GitHub Pre-Release
        if: env.SKIP_RELEASE != 'true'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.VERSION_TAG }}
          name: ${{ env.VERSION_TAG }}
          prerelease: true
          replace_existing_artifacts: true
          files: dist/*
          body_path: RELEASE_NOTES.md
